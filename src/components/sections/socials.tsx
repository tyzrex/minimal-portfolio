import Projects from "../molecules/socials";
import SectionWrapper from "../wrappers/section-wrapper";

export default function Socials() {
  return (
    <SectionWrapper>
      <Projects />
    </SectionWrapper>
  );
}
